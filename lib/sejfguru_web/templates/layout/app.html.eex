<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Sejfguru!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <header class="header">
      <nav class="navbar navbar-inverse navbar-expand">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="/">Sejfguru</a>
          </div>
          <ul class="nav navbar-nav">
            <li class="active"><a href="<%= page_path(@conn, :protected) %>">Assets</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <%= if assigns[:current_user] do %>
              <li><a href="#">Hello, <%= @current_user.first_name %></a></li>
            <% else %>
              <li><a href="<%= auth_path(@conn, :request) %>">Login with Google</a></li>
            <% end %>
          </ul>
        </div>
      </nav>
      <span class="logo"></span>
    </header>
    <div class="container">
      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
